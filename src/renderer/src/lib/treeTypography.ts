/** Sidebar tree, tabs, and compact UI control labels (apply on the element that shows text). */
export const TREE_LABEL_CLASS = "text-[14px] font-normal leading-snug";

/** Workspace root row — same size as files, slightly lower contrast. */
export const TREE_ROOT_LABEL_CLASS = `${TREE_LABEL_CLASS} uppercase tracking-wide text-[#82828a]`;
