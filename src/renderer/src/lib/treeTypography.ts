/** Sidebar tree, tabs, and compact UI control labels (apply on the element that shows text). */
export const TREE_LABEL_CLASS = "text-[13px] font-normal leading-[22px]";

/** Panel chrome titles (explorer header, agent header, window chrome label). */
export const CHROME_TITLE_CLASS = "text-[13px] font-medium leading-[22px]";

/** Secondary chrome lines (agent status, empty states, PDF footer). */
export const CHROME_META_CLASS = "text-[11px] font-normal leading-[16px]";

/** Uppercase section labels (empty-thread state, thinking summary). */
export const CHROME_SECTION_CLASS =
  "text-[11px] font-semibold uppercase tracking-wider text-text-muted/80";

/** Agent message body — same rhythm as editor (13px / 22px). */
export const AGENT_MESSAGE_CLASS = TREE_LABEL_CLASS;
